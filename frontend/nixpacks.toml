[variables]
NIXPACKS_NODE_VERSION = "22"
VITE_API_URL = "https://api.trasoftvps.xyz"

[phases.install]
cmds = ["npm ci"]

[phases.build]
cmds = ["npm run build"]

[start]
cmd = "npx --yes serve dist -s -l tcp://0.0.0.0:${PORT:-3000}"
